
[project]
name = "zpay-upload"
version = "0.2.0"
requires-python = ">=3.10"
dependencies = [
  "fastapi",
  "uvicorn",
  "pandas",
  "openpyxl",
  "pdfplumber",
  "pypdf",
]

[tool.zpay_upload]
description = "Clean, modular FastAPI upload handler with robust CSV/Excel/PDF parsing."
